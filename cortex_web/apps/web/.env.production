# Production build-time env for the SPA (Vite loads this during `vite build`).
# VITE_GOOGLE_CLIENT_ID is the PUBLIC Google OAuth Web client ID baked into the
# bundle so the "Continue with Google" button renders. Must match the backend's
# GOOGLE_CLIENT_ID (the audience the ID token is verified against).
VITE_GOOGLE_CLIENT_ID=238967754738-pl3q8c40pcvnn94tvvirnn8si6hsisaf.apps.googleusercontent.com
