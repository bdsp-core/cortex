export const eegDomains = Object.freeze([
  Object.freeze({
    key: "spike",
    label: "Spike",
    color: "--domain-spike",
    seed: 1701,
    target: Object.freeze({ skill: 1.62, bias: -.42 }),
    auroc: Object.freeze({ experts: .958, nonexperts: .729 }),
  }),
  Object.freeze({
    key: "seizure",
    label: "Seizure",
    color: "--domain-seizure",
    seed: 2909,
    target: Object.freeze({ skill: 1.27, bias: -.08 }),
    auroc: Object.freeze({ experts: .952, nonexperts: .721 }),
  }),
  Object.freeze({
    key: "lpd",
    label: "LPD",
    color: "--domain-lpd",
    seed: 3917,
    target: Object.freeze({ skill: 1.02, bias: .38 }),
    auroc: Object.freeze({ experts: .944, nonexperts: .713 }),
  }),
  Object.freeze({
    key: "gpd",
    label: "GPD",
    color: "--domain-gpd",
    seed: 4933,
    target: Object.freeze({ skill: .76, bias: .72 }),
    auroc: Object.freeze({ experts: .938, nonexperts: .704 }),
  }),
  Object.freeze({
    key: "lrda",
    label: "LRDA",
    color: "--domain-lrda",
    seed: 5953,
    target: Object.freeze({ skill: .58, bias: -.66 }),
    auroc: Object.freeze({ experts: .933, nonexperts: .697 }),
  }),
  Object.freeze({
    key: "grda",
    label: "GRDA",
    color: "--domain-grda",
    seed: 6961,
    target: Object.freeze({ skill: .34, bias: .12 }),
    auroc: Object.freeze({ experts: .927, nonexperts: .688 }),
  }),
  Object.freeze({
    key: "other",
    label: "Other",
    color: "--domain-other",
    seed: 7993,
    target: Object.freeze({ skill: .08, bias: .94 }),
    auroc: Object.freeze({ experts: .921, nonexperts: .681 }),
  }),
]);
