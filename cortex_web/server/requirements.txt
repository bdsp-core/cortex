# CORTEX web backend — intentionally minimal.
# Auth (PBKDF2 + HS256 JWT) and storage (SQLite) use only the stdlib, so the
# only third-party deps are the web framework and the dev/test tooling.
fastapi>=0.110
uvicorn[standard]>=0.27
python-multipart>=0.0.9   # /api/videos multipart upload (trajectory blobs)
# Visualization videos (/api/videos) run the desktop renderers
# (scripts/cortex_render_videos.py + render_engine_explainer.py). Heavy, but the
# repo venv already provides them; install here if running the API standalone:
numpy>=1.26
matplotlib>=3.8
imageio-ffmpeg>=0.5       # bundles the ffmpeg binary
# Tests:
httpx>=0.27          # FastAPI TestClient transport
pytest>=8.0
