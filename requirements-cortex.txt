# CORTEX internal test — runtime dependencies.
#
# The minimal subset of the repo's requirements.txt that the CORTEX
# test-taker app actually imports — no numpyro/jax, no calibration or
# figure-generation packages. numpy/scipy/pandas/h5py/PyYAML are pinned
# to match the repo's reproducibility set; the GUI/upload deps are
# floor-pinned (they do not affect the engine numerics).
#
# matplotlib drives the per-test-taker collapse.mp4 + passfail.mp4
# visualizations rendered at the end of each session via
# scripts/cortex_render_videos.py. v1.1.2 bundles ffmpeg via
# imageio-ffmpeg (per-platform binaries inside the wheel — ~25 MB
# Linux, ~75 MB mac/win) so MP4 rendering works on machines without
# a system ffmpeg install. cortex_render_videos points
# matplotlib.rcParams['animation.ffmpeg_path'] at the bundled binary
# at import time. imageio-ffmpeg's bundled ffmpeg is GPL (libx264).
numpy==1.26.4
scipy==1.13.1
pandas==2.2.2
h5py==3.11.0
PyYAML==6.0.2
matplotlib>=3.7
imageio-ffmpeg>=0.5
PyQt6>=6.5
pyqtgraph>=0.13
dropbox>=11.36
