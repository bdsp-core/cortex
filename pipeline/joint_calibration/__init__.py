"""Phase 3.5 — joint hierarchical cross-source s_j calibration (NumPyro).

Calibration-stage only; NOT imported by the engine runtime (the engine's
numpy single-thread-BLAS bit-exact contract is unaffected). See
docs/DATA_UNIFICATION_ANALYSIS.md (3-agent adjudication) and the merge
plan Phase 3.5.
"""
