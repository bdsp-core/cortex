# Data Provenance — Phase 0 placeholder

Populated in Phase 1 (plan): full chain raw annotations → Rasch ℓ
(c_j/l_i, logit) → ×1/1.7 → probit-lapse MLE (σ̂,θ̂; λ=0.025) → Youden ℓ*/σ*
→ engine_inputs/ + deployment_prior/. Records the five integrity remediations
(R1 stale segment_labels.csv, R2 rebuild consolidated/, R3 Centaur
triple-representation, R4 legacy Sigma_l, R5 rater_id namespace) and R6
(K=6→7, the real `other`/iic task).
