# Publication code-cleanup manifest → moved to PROJECT_CORPUS.md §17

> **The canonical manifest now lives in `docs/PROJECT_CORPUS.md` §17** ("Publication code-cleanup
> manifest"), integrated 2026-06-08 per Eli's "unified memory" directive (one cohesive corpus).
> This file is kept only as a thin pointer so the in-code comments that reference this path —
> `grep -rn "PUB-CLEANUP\[estimation-ladder\]" engine/` — still resolve.
>
> Go to **PROJECT_CORPUS.md §17** for: the curated-export strategy, the §A already-excluded list,
> the §B estimation-ladder hooks to strip (tagged `PUB-CLEANUP[estimation-ladder]`), the §C
> PI-decision scaffolding (`engine/variants/`, `scripts/run_*.py`/`diag_*.py`, sim-only tests),
> and the export checklist.
