# Learning workstream (moved)

The "learning with feedback" design note that briefly lived here was
relocated to its own repo:

> <https://github.com/bdsp-core/ilae-skill-certification-learning>

Specifically the rendered design memo is at
[`docs/learning_with_feedback_design_note.tex`](https://github.com/bdsp-core/ilae-skill-certification-learning/blob/main/docs/learning_with_feedback_design_note.tex)
(plus the `.pdf` for non-TeX readers).

The two source markdowns (`learning-theory{1,2}.md`) that note
synthesises were never in this repo — they originated in
[`bdsp-core/ideal-test/docs/`](https://github.com/bdsp-core/ideal-test/tree/main/docs)
and have now been copied into the new repo's `source-markdowns/`.

That new repo's `PARENT_REPO_BACKGROUND.md` documents which parts of
*this* repo it draws on (CORTEX engine, calibration, the test bank
on S3, the labels tables) — handy if the trainer ever needs to be
re-rooted against an updated parent.
